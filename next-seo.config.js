const title = 'Shop';
const description = 'Shop description';

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: 'Shop',
  },
};

export default nextSeoConfig;
